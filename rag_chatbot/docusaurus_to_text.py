import re\nfrom bs4 import BeautifulSoup\n\ndef convert_docusaurus_markdown_to_text(markdown_content: str) -> str:\n    \"\"\"Converts Docusaurus-flavored Markdown (MDX) to plain text, preserving headings minimally.\n\n    This function aims to strip out Markdown and JSX elements while retaining a readable\n    representation of the content for embedding.\n    \"\"\"\n    # 1. Remove Docusaurus/MDX frontmatter (YAML block at the top)\n    if markdown_content.startswith(\'---\'):\n        parts = markdown_content.split(\'---\\n\', 2)\n        if len(parts) > 2:\n            markdown_content = parts[2]\n        else:\n            # Malformed frontmatter or only frontmatter\n            markdown_content = \"\"\n\n    # 2. Remove JSX components and imports (simplistic approach)\n    # This regex tries to match <ComponentName ... /> or <ComponentName>...</ComponentName>\n    # and also import statements\n    markdown_content = re.sub(r\'import .* from .*;\\n\', \'\', markdown_content)\n    markdown_content = re.sub(r\'<[A-Z][a-zA-Z0-9]*[^>]*>.*?<\/[A-Z][a-zA-Z0-9]*>|\n                               <[A-Z][a-zA-Z0-9]*[^>]*\/>\', \'\', markdown_content, flags=re.DOTALL)\n\n    # 3. Convert remaining Markdown to plain text using BeautifulSoup\n    # (A common trick is to convert markdown to HTML and then strip HTML)\n    # For this, we\'ll use a basic approach or a markdown library if available.\n    # As we don\'t have a markdown-to-html converter directly, we\'ll do a regex-based stripping.\n\n    # Remove code blocks\n    markdown_content = re.sub(r\'```.*?```\', \'\', markdown_content, flags=re.DOTALL)\n    # Remove inline code\n    markdown_content = re.sub(r\'`[^`]+`\', \'\', markdown_content)\n    # Remove images\n    markdown_content = re.sub(r\'!\\[.*\\]\\(.*\)\', \'\', markdown_content)\n    # Remove links (keep link text)\n    markdown_content = re.sub(r\'\\[(.*?)\\]\\(.*?\\)\', r\'\\1\', markdown_content)\n    # Remove bold/italic markers\n    markdown_content = re.sub(r\'(\\*\\*|_)(.*?)\\1\', r\'\\2\', markdown_content)\n    markdown_content = re.sub(r\'(\\*|_)(.*?)\\1\', r\'\\2\', markdown_content)\n    # Remove blockquotes\n    markdown_content = re.sub(r\'^>\\s*\'\n, \'\', markdown_content, flags=re.MULTILINE)\n    # Replace headings with just their text, followed by two newlines for separation\n    markdown_content = re.sub(r\'^#+\\s*(.*)\', r\'\\1\\n\\n\', markdown_content, flags=re.MULTILINE)\n    # Remove horizontal rules\n    markdown_content = re.sub(r\'^---\\s*$\', \'\', markdown_content, flags=re.MULTILINE)\n    # Remove list markers (simple, assumes single character like * or -)\n    markdown_content = re.sub(r\'^\\s*[\\*\\-+]\\s+\', \'\', markdown_content, flags=re.MULTILINE)\n    # Remove numbered list markers (simple)\n    markdown_content = re.sub(r\'^\\s*\\d+\\.\\s+\', \'\', markdown_content, flags=re.MULTILINE)\n\n    # Replace multiple newlines with single newlines\n    markdown_content = re.sub(r\'\\n+\', \'\\n\', markdown_content).strip()\n\n    return markdown_content\n